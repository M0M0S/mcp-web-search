# =============================================================================
# MCP Configuration
# =============================================================================
MCP_NAME=web-search
MCP_VERSION=1.0.0
MCP_HOST=0.0.0.0
LOG_LEVEL=INFO

# =============================================================================
# Redis Configuration
# =============================================================================
REDIS_URL=redis://localhost:6379/0
SEARCH_RESULT_CACHE_TTL=3600
CONTENT_CACHE_TTL=86400
WEBFETCH_CACHE_TTL=1800

# =============================================================================
# Search Configuration
# =============================================================================
DEFAULT_SEARCH_PROVIDER=duck
USE_GOOGLE_FALLBACK=false
SEARCH_FALLBACK_CHAIN=["duck","searxng","tavily","google"]
MAX_RESULTS=10

# =============================================================================
# Smart Filter Thresholds
# =============================================================================
QUALITY_SCORE_THRESHOLD=0.6
BLACKLIST_DOMAINS=["example.com"]

# =============================================================================
# get_content Configuration
# =============================================================================
TOKEN_LIMIT=8000

# =============================================================================
# webfetch (LangGraph) Configuration
# =============================================================================
FEATURE_LLM_MODEL=gpt-4o-2025-04
JUDGE_URL_THRESHOLD=0.85
JUDGE_FEATURES_THRESHOLD=0.92
MAX_CONCURRENT=6

# =============================================================================
# API Keys (optional - for fallback providers)
# =============================================================================
TAVILY_API_KEY=
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# =============================================================================
# LLM Webfetch Settings (feature collection)
# =============================================================================
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=gpt-4o-2025-04

# =============================================================================
# LLM Model Failover Chain
# =============================================================================
LLM_MODEL_FALLBACK_CHAIN=["gpt-4o-2025-04","gpt-4o","gpt-4"]  # ordered fallback chain (comma-separated)
LLM_MODEL_FALLBACK_BASE_URLS={"claude-3-sonnet":"https://api.anthropic.com/v1"}  # per-model base_url override (optional, JSON)
LLM_HEALTH_WINDOW=10  # health tracking window (number of recent calls)
LLM_HEALTH_FAILURE_THRESHOLD=0.5  # failure threshold (0.0–1.0)

# =============================================================================
# Provider Health Tracker
# =============================================================================
PROVIDER_HEALTH_WINDOW=10  # health tracking window (number of recent probes)
PROVIDER_HEALTH_FAILURE_THRESHOLD=0.5  # failure threshold (0.0–1.0)
PROVIDER_HEALTH_PROBE_INTERVAL=30  # probe interval in seconds
PROVIDER_COOLDOWN_PERIOD=300  # cooldown in seconds after failure detection

# =============================================================================
# Search Configuration Defaults
# =============================================================================
DEFAULT_REGION=wt-wt
DEFAULT_LANGUAGE=en

# =============================================================================
# Cache Warming URLs (optional)
# =============================================================================
WARM_CACHE_URLS=

# =============================================================================
# SearxNG Configuration (NEW)
# =============================================================================
SEARXNG_BASE=  # optional, public SearxNG instance (e.g. https://searx.workers.dev)

# =============================================================================
# WebFetch Configuration
# =============================================================================
GEN_SRCH_Q_CNT=5  # Number of search queries to generate (3-10)

# =============================================================================
# LLM-as-Judge Configuration
# =============================================================================
SKIP_JUDGE=false  # Skip LLM judge for trusted sites (docs.python.org, etc.)

# =============================================================================
# MCP Authorization Configuration
# =============================================================================
MCP_ENCRYPTION_KEY=  # Fernet encryption key (required for auth)
MCP_ENCRYPTION_KEY_BACKUP=  # Backup encryption key (optional, for recovery)
ADMIN_KEY_IDS=  # Comma-separated key_ids with admin scope (e.g. key_abc123,key_def456)

# =============================================================================
# Rate Limit Defaults
# =============================================================================
DEFAULT_RATE_LIMIT_DAILY=100
DEFAULT_RATE_LIMIT_WEEKLY=500
DEFAULT_RATE_LIMIT_MONTHLY=2000

# =============================================================================
# Token Limit Defaults
# =============================================================================
DEFAULT_TOKEN_LIMIT_DAILY=  # None = unlimited
DEFAULT_TOKEN_LIMIT_WEEKLY=  # None = unlimited
DEFAULT_TOKEN_LIMIT_MONTHLY=  # None = unlimited
